{
  "name": "novachat-ai",
  "description": "NovaChat AI: An advanced intelligent PDF conversational agent powered by LangChain and LangGraph.",
  "private": true,
  "version": "1.0.0",
  "author": "Aftab",
  "license": "MIT",
    "workspaces": [
      "backend",
      "frontend"
    ],
    "scripts": {
      "build": "yarn run turbo:command build",
      "turbo:command": "yarn turbo",
      "lint": "yarn run turbo:command lint",
      "lint:fix": "yarn run turbo:command lint:fix", 
      "format": "yarn run turbo:command format",
      "format:check": "yarn run turbo:command format:check"
    },
    "devDependencies": {
      "turbo": "latest"
    }
  }